## 1. 全量树与叶子视图

- [x] 1.1 在事件字典路径实现父事件判定：`event_id` 出现在任一非空 `parent_id` 集合中即为父
- [x] 1.2 缓存保留全量事件树，并派生叶子视图供匹配候选与落库使用
- [x] 1.3 确认 24h TTL 拉取与 `_sync_vector_store_if_changed` 仍基于刷新后的全量数据工作，同步时不删 `source=user`

## 2. 父命中强制消歧

- [x] 2.1 实现父名检测（精确匹配父 `event_name` / 约定名称字段）并生成子选项与澄清问句
- [x] 2.2 向量匹配结果若为父事件，拦截并改写为消歧 pending，禁止高置信直接 END
- [x] 2.3 LLM 分类结果若为父事件，同样改写为消歧 pending，禁止直接落库
- [x] 2.4 落库前增加叶子校验：父 `event_id` 不得作为最终 feeding 结果

## 3. 同一 /intent 续聊与自由文本解析

- [x] 3.1 扩展意图请求/响应契约：请求可选 `conversation_id`；消歧响应返回问句、`options`、`conversation_id`
- [x] 3.2 实现 pending 会话状态（父、子选项、上一问句）；`/intent` 发现 pending 则走澄清解析而非冷启动
- [x] 3.3 实现自由文本分层解析：子名/`extra_names` → 序号 → 拒绝词 → 模糊再问 → **其余任意打字清 pending 当新意图**
- [x] 3.4 删除旧 `/intent/confirm` 与 `confirm|reject` 主路径及相关死代码，按新逻辑接线意图图

## 4. 飞轮与向量治理

- [x] 4.1 消歧成功落到唯一叶子后，将消歧前用户原话关联到该叶子写入飞轮；短回复不单独入库
- [x] 4.2 同步/初始化后移除或禁止父类标准向量作为可落库命中
- [x] 4.3 确保父事件在任何路径都不会触发飞轮写入

## 5. 验证与收尾

- [x] 5.1 核对实现与更新后的 spec 一致：任意离题打字当新意图；父命中强制消歧；只落叶子
- [x] 5.2 冒烟：父名 → 选项 → 子名/序号落地；拒绝词取消；随意离题句当新意图
- [x] 5.3 冒烟：叶子高置信可直接结果；向量/LLM 父命中被改写为消歧
- [x] 5.4 确认 24h 缓存路径与飞轮写入仍有效；无 `/intent/confirm` 主路径残留
