---
description: 禁止生成或改动测试文件（除非用户明确要求）
alwaysApply: true
---

# 禁止测试文件

除非用户**明确要求**编写或修改测试，否则：

- **禁止**创建任何测试文件（如 `tests/**`、`test_*.py`、`*_test.py`、`*_spec.py` 等）
- **禁止**为对齐断言而修改既有测试文件
- **禁止**在 OpenSpec `tasks.md` 中写入「编写测试 / 创建测试 / 对齐断言」类任务
- **禁止**主动建议补充测试作为验收手段（用 spec Scenario / 手工验证即可）

原因：本仓库不使用测试文件；行为以 OpenSpec 基线与人工验收为准。
